Setup with uv:

```
make install
. .venv/bin/activate
make fix
make check
```

Run your project:

```
python -m src.main
```

See `EXPLANATION.md` for details about the template structure and tooling.
